{
  "name": "@aqlamna/runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Aqlamna runtime player — zero dependencies, embeddable in standalone HTML",
  "license": "GPL-3.0-only",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "node scripts/build-runtime.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  }
}
